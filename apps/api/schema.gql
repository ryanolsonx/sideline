# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

enum AssignmentStatus {
  OUT
  PLAYING
}

input CreateMatchInput {
  name: String!
}

input CreateTeamInput {
  name: String!
  players: [String!]!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Game {
  currentRound: Int!
  fieldSize: Int!
  id: ID!
  rounds: [GameRound!]!
  startedAt: DateTime!
  status: GameStatus!
  team: Team!
}

type GameRound {
  assignments: [GameRoundAssignment!]!
  id: ID!
  number: Int!
}

type GameRoundAssignment {
  id: ID!
  player: Player!
  position: Position
  status: AssignmentStatus!
}

enum GameStatus {
  ACTIVE
  COMPLETE
}

type Match {
  createdAt: DateTime!
  id: ID!
  name: String!
}

type Mutation {
  advanceGame(gameId: String!): Game!
  createMatch(input: CreateMatchInput!): Match!
  createTeam(input: CreateTeamInput!): Team!
  startGame(input: StartGameInput!): Game!
}

type Player {
  id: ID!
  name: String!
}

enum Position {
  DEFENDER
  FORWARD
  GOALIE
}

type Query {
  activeGame(teamId: String!): Game
  matches: [Match!]!
  teams: [Team!]!
}

input StartGameInput {
  fieldSize: Int!
  presentPlayerIds: [ID!]!
  teamId: ID!
}

type Team {
  createdAt: DateTime!
  id: ID!
  name: String!
  players: [Player!]!
}